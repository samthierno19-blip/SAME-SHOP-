const form=document.getElementById('form'),signupTab=document.getElementById('signupTab'),loginTab=document.getElementById('loginTab'),title=document.getElementById('title'),submit=document.getElementById('submit'),signupFields=document.getElementById('signupFields'),message=document.getElementById('message');let loginMode=false;
function setMode(login){loginMode=login;title.textContent=login?'Connexion':'Créer mon compte';submit.textContent=login?'Se connecter':'Créer mon compte';signupFields.classList.toggle('hidden',login);signupTab.classList.toggle('active',!login);loginTab.classList.toggle('active',login);message.textContent='';document.getElementById('password').autocomplete=login?'current-password':'new-password'}
signupTab.onclick=()=>setMode(false);loginTab.onclick=()=>setMode(true);
form.onsubmit=async e=>{e.preventDefault();message.textContent='Chargement…';const email=document.getElementById('email').value.trim(),password=document.getElementById('password').value;try{if(loginMode){const{error}=await sameShopSupabase.auth.signInWithPassword({email,password});if(error)throw error;location.href='client.html';return}const name=document.getElementById('name').value.trim(),phone=document.getElementById('phone').value.trim();if(!name||!phone){message.textContent='Veuillez renseigner votre nom et votre téléphone.';return}const{data,error}=await sameShopSupabase.auth.signUp({email,password,options:{data:{full_name:name,phone}}});if(error)throw error;if(data.user){const r=await sameShopSupabase.from('profiles').upsert({id:data.user.id,full_name:name,phone},{onConflict:'id'});if(r.error)console.warn(r.error)}if(data.session)location.href='client.html';else message.innerHTML='✅ Compte créé. Vérifiez votre adresse e-mail pour confirmer votre compte.'}catch(error){message.textContent=error.message||'Une erreur est survenue.'}};
setMode(false);